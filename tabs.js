
$(document).ready(function(){
	if ($(document.body).hasClass("portaltype-uom-pfiles-profile")) {
		alert("test");
	  $("#tabs").profiletabs({ labelledBy: "tabsLbl" });
	  // Adds profile tabs and ARIA for tabs
	}
})

